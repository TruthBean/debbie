/*
package com.truthbean.debbie.controversial;

import com.truthbean.debbie.core.ApplicationContext;
import com.truthbean.debbie.properties.PropertyInject;
import com.truthbean.debbie.reflection.FieldInfo;

import java.lang.reflect.Method;

interface InjectedBeanFactory {

    <T> T factory(FactoryBeanInfo<T> factoryBeanInfo);

    <T> T factory(FactoryBeanInfo<T> factoryBeanInfo, boolean skipFactory);

    <T> T factory(FactoryBeanInfo<T> factoryBeanInfo, boolean skipFactory, Object firstParamValue);

    <T> T factory(ReflectionBeanCreator<T> creator);

    <T> T factory(ReflectionBeanCreator<T> creator, Object firstParamValue);

    <T> ReflectionBeanCreator<T> factoryBeanPreparation(FactoryBeanInfo<T> factoryBeanInfo, boolean skipFactory);

    void resolveFieldValue(FactoryBeanInfo<?> factoryBeanInfo, FieldInfo field, String keyPrefix);

    void resolveAwareValue(ApplicationContext applicationContext, Object object, Class<?> clazz);

    void resolveMethodValue(Object object, Method method);

    Object factoryProperty(Class<?> valueType, String keyPrefix, PropertyInject propertyInject);

    void destroy();
}
*/
